#ifndef LEXER_H
#define LEXER_H

#include <string> //because lexer will work with std::string
#include <vector> //imports the vector

//this says that the lexer function will take a string input, and return a vector of strings
std::vector < std::string > lexer(std::string inputFunction); 

#endif